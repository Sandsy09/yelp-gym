<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Gym</title>
</head>
<body>
    <h1>Edit Gym</h1>

    <form action="/gyms/<%= gym.id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" name="gym[title]" id="title" value="<%= gym.title %>">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" name="gym[location]" id="location" value="<%= gym.location %>">
        </div>
        <div>
            <label for="price">Price</label>
            <input type="text" name="gym[price]" id="price" value="<%= gym.price %>">
        </div>
        <button>Update Gym</button>
    </form>
    
</body>
</html>